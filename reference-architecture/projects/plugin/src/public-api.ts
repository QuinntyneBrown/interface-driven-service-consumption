/*
 * Public API Surface of plugin
 *
 * Plugin libraries expose only their UI artifacts (components, directives,
 * pipes). Their implementations consume framework contracts via injection,
 * so consumers don't need to wire anything beyond the framework providers.
 */

export * from './lib/dashboard/dashboard';
